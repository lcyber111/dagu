services:
  caddy-gateway:
    image: caddy:2.11.4-alpine
    container_name: caddy-gateway
    # 以宿主 uid/gid 运行，保证 access.log 归宿主用户所有（reap_idle 需要读）
    user: "${GATEWAY_UID:-1000}:${GATEWAY_GID:-1000}"
    restart: unless-stopped
    ports:
      - "9088:9088"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      # 访问日志目录（宿主机路径由 install.sh 写入 gateway/.env 的 LOG_DIR）
      - ${LOG_DIR:-./logs}:/logs
    environment:
      START_PAGE_REFRESH: ${START_PAGE_REFRESH:-5}
    networks:
      - dagu-net

networks:
  dagu-net:
    external: true
